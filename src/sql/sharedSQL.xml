<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="shared">

	<select id="selectArticles" parameterType="hashmap" resultType="groupSpaceDTO">
		<![CDATA[
		SELECT B.*, r FROM
		(SELECT A.*, rownum r FROM 
		(SELECT * FROM board ORDER BY ref DESC, re_step ASC) A 
		ORDER BY ref DESC, re_step ASC) B WHERE r >= #{start} AND r <= #{end}
		]]>
	</select>
	
	<select id="selectOne" parameterType="int" resultType="GalleryDTO">
		select * from groupSpace where num = #{value}
	</select>
  		
</mapper>